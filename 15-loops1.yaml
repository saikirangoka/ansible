- name: installing packages
  hosts: webs
  become: yes
  tasks:
  - name: installing packages
    ansible.builtin.package:
      name: "{{ item }}"
      state: installed
    loop:
    - git
    - mysql
    - gcc
    - zip